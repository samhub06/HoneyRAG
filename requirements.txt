# Core workflow orchestration
prefect==2.10.0

# Dashboard / UI
streamlit==1.25.0

# Vector database for embeddings
faiss-cpu==1.12.0

# Embedding models
sentence-transformers==2.2.2

# Local LLM integration (latest available on PyPI)
gpt4all==0.1.7

# Supporting libraries
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0
requests==2.32.3

Save this as requirements.txt in your repo root (HoneyRAG/).

Run:

powershell
pip install -r requirements.txt

Verify installation:

powershell
pip list
